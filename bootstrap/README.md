# Development environment updates

The global `update-dev` command lives in
[`bin/.local/scripts`](../bin/.local/scripts/README.md#development-environment-updates).

```sh
update-dev --dry-run
update-dev
```
